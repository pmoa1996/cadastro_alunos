const {resolve} = require('path');

module.exports = {
  config : resolve(__dirname, 'src', 'config', 'database.js'),   // Caminho para o arquivo de configuração do banco de dados
  'models-path': resolve(__dirname, 'src', 'models'),   // Caminho para os arquivos dos modelos da aplicação
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),   // Caminho para os arquivos de migrações do banco de dados
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),   // Caminho para os arquivos de seeders
}
